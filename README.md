# mensetsu

## CSS運用

- CSS命名規則と配置ルールは `[CSS_NAMING_RULES.md](./CSS_NAMING_RULES.md)` を参照
- 新規ページ作成時は `common.css` -> 章トップCSS -> 必要時のみ個別CSS の順で設計する
- レイアウト判断は `index.html` を全体基準、各章の1ページ目を章基準として行う
- 作成前に「全体共通か、章共通か、ページ固有か」を必ず先に判定する
