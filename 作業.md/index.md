# index.html 作業記録

## こちらの要望

- 全体の作りは現在の状態に合わせて作成する
- セマンティックなHTMLに変える
- ブレイクポイントを以下で指定する
- モバイル: 〜767px
- タブレット: 768px〜1023px
- PC: 1024px以上
- 最小widthサイズは360px
- 基本的に画面下部のスライドバーは出ない設計にする
- `index.html` を本題材のトップページとして作成する
- 各大項目に飛べるように構成する
- 下部のナンバーは `0 / 総ページ数` とする
- 現在の1/13と2/13をあわせた内容を `index.html` にする
- タブレット〜PC幅では左上カラム、左下カラム、右側カラム構成にする
- モバイル幅以下では右側カラムを非表示にする
- 1〜5とまとめは、それぞれ内容にあったHTMLファイルを作成する
- CSSは `css` フォルダ配下に共通CSSと個別CSSを作成する
- スライド方式も保つ
- フッターを【C:\Users\hiita\src\Web面接\presentation.html】下部のような見た目にする
- セクションHTML/CSSのファイル名を英語名称に変更する
- `目次` を上下中央寄せにする
- `0 / 総ページ数` も上下中央寄せにする
- SP幅でも `link-list` は横並びを維持する
- ヘッダー右側にも `pager-main` を配置する
- `← → キーでも操作できます` が実際に機能するようにする
- ここまでで `index.html` は完成とする

## 作業内容

- `index.html` をトップページとして新規作成
- `pre-interview-checks.html`
- `interview-day.html`
- `materials-preparation.html`
- `chatgpt-support.html`
- `final-checklist.html`
- `summary-conclusion.html`
- 上記各HTMLをスライド1枚単位の見た目で新規作成
- `css/common.css` を作成し、全体レイアウト、カード、ヘッダー、フッター、ページャーを共通化
- 各ページ専用CSSを作成
- `css/index.css`
- `css/pre-interview-checks.css`
- `css/interview-day.css`
- `css/materials-preparation.css`
- `css/chatgpt-support.css`
- `css/final-checklist.css`
- `css/summary-conclusion.css`
- ブレイクポイントをモバイル、タブレット、PCで整理
- `min-width: 360px` を設定
- モバイル時はトップページ右カラム画像を非表示化
- フッターを `前へ / ページ番号 / 次へ / 進捗バー / キー操作案内` の構成へ変更
- ヘッダー右側にも `pager-main` を追加
- 共通JSとして `js/slide-navigation.js` を作成し、左右キーで前後ページへ移動できるようにした
- 全HTMLから共通JSを読み込むようにした

## インシデントの有無

- あり

## その解決策

- `apply_patch` で一括追加しようとした際、Windows 側でコマンド長起因のエラーが発生した
- 追加対象を分割し、複数回に分けて作成した
- ターミナル上ではHTML/Markdownの日本語が文字化けして見える場面があった
- ユーザー環境では正常表示との確認を取ったうえで、文言破損を前提にせず作業を継続した
- `← → キーでも操作できます` 表示のみで実際には動作していなかった
- `js/slide-navigation.js` を作成し、左右キー入力で前後ページへ遷移するように修正した

## 追記: 今回の要望

- `index.html` のタブレット幅レイアウトを変更する
- `card index-intro` を1ロウにし、その下に `stack` と `image-panel hero-visual` を2カラムで並べる
- 画像は左カラムの高さに合わせ、下部は見切れてもよい
- PC幅では左側に `index-intro` と目次、右側に画像という構成にする
- 冗長構成になっていないか確認する
- `common.css` を共通土台として維持する方針を確認する
- 現状はこのままで進める

## 追記: 今回の作業内容

- `index.html` のトップ構造をPC、タブレット、SPで切り替えやすい構成へ再整理
- `css/index.css` を見直し、PC幅、タブレット幅、SP幅ごとのグリッドエリアを明示
- タブレット幅では上段に `index-intro`、下段に目次と画像の2カラムになるよう調整
- PC幅では左側に `index-intro` と目次、右側に画像が来るよう調整
- SP幅では既存どおり画像非表示、縦積みを維持
- `index.html` と `index.css` の責務を整理し、レイアウト制御は `index.css` 側へ寄せた
- `common.css` は今後の他ページ調整でも使う共通土台として維持する方針を確認した
- 現状のHTML構造とCSS構成が過度に冗長ではないか確認した

## 追記: 今回のインシデントの有無

- あり

## 追記: 今回のインシデント対応

- `index.html` の構造調整中に、ターミナル上の表示上の文字化けではなく、実ファイル側でも文字化けが発生した
- ユーザーに表示状況を確認し、ユーザー環境で実害があることを確認したうえで対応した
- `index.html` をUTF-8前提で正常な文言と構造に復元した
- その後もPC幅レイアウトが意図どおり反映されなかったため、構造だけでなく `css/index.css` のメディアクエリ競合も見直した
- PC幅で右カラム画像が落ちる原因は、PC用指定の後に広い条件の1カラム指定が当たっていたことだったため、条件を整理して解消した
- 最終的に、PC、タブレット、SPでそれぞれ意図どおりのレイアウトになるよう修正した

## 追記: 今回の要望

- `pre-interview-checks.html` のタイトルサイズを `index.html` に合わせる

## 追記: 今回の作業内容

- `pre-interview-checks.html` の見出しクラス変更を試みた
- その後、破損した `pre-interview-checks.html` を最新の正常構成で復元した
- 復元後、UTF-8 で再読込して日本語文言、画像パス、前後リンク、JS 読み込みを確認した

## 追記: 今回のインシデントの有無

- あり

## 追記: 今回のインシデント対応

- `pre-interview-checks.html` の一部置換時に文字コードが崩れ、実ファイル側で文字化けが発生した
- 影響範囲を切り分け、`index.html` と `重要.md` は正常であることを確認した
- 破損していた `pre-interview-checks.html` のみを対象に、UTF-8 前提で全体を復元した
- 今後、HTML の日本語文言を含む置換は PowerShell の置換書き戻しで進めず、まず影響範囲確認をしたうえで `apply_patch` を優先する
